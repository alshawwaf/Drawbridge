# Check Point Dynamic-Object Integration Simulator
# All deps are MIT / BSD / Apache-2.0 — no GPL (proprietary-module safe).
fastapi>=0.115
uvicorn>=0.34
pydantic>=2.9
pydantic-settings>=2.5
SQLAlchemy>=2.0
Jinja2>=3.1
python-multipart>=0.0.12
itsdangerous>=2.2
httpx>=0.27
# AES-256-GCM at-rest encryption for the optional saved gateway password (Apache-2.0/BSD).
# Optional at runtime: if absent, the app boots and falls back to a per-apply password.
cryptography>=43.0
# MCP server for n8n / LLM agents (MIT). Optional at runtime: if absent, the app boots and /mcp is simply
# not mounted (import-guarded in app/mcp_server.py). Install via Artifactory.
mcp>=1.10
